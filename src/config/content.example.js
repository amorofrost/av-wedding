// ─────────────────────────────────────────────────────────────────────────────
// Шаблон файла с личными данными свадьбы.
//
//   cp src/config/content.example.js src/config/content.local.js
//
// ...и впишите свои значения. content.local.js не попадает в git, поэтому имена,
// даты и адрес площадки не окажутся в публичном репозитории. Без этого файла
// приложение не стартует — так нельзя случайно показать гостям заглушки.
//
// Перечислять нужно только то, что переопределяете: объекты сливаются по ключам
// (можно задать один couple.bride), массивы заменяются целиком.
// Остальное берётся из значений по умолчанию в content.js.
// ─────────────────────────────────────────────────────────────────────────────

export default {
  couple: {
    // Порядок отображения в заголовке: bride & groom
    bride: 'Имя невесты',
    groom: 'Имя жениха',
    // Короткие инициалы для логотипа (напр. «А & В»)
    monogram: 'И & И',
  },

  event: {
    // ISO-дата начала церемонии — используется для обратного отсчёта.
    // Держите её в согласии с dateHuman: они не связаны автоматически.
    dateISO: '2030-06-15T16:00:00+03:00',
    // Как дата и время показываются гостям
    dateHuman: '15 июня 2030',
    timeHuman: '16:00',
    dayOfWeek: 'суббота',
    city: 'Город',
  },

  // Крайний срок ответа на приглашение (RSVP)
  rsvpDeadline: {
    dateISO: '2030-05-01T23:59:59+03:00',
    dateHuman: '1 мая 2030',
  },

  rsvp: {
    // Значения (value) менять нельзя — они уходят в хранилище и сверяются
    // в routes/invite.js. Здесь уточняются только подписи для гостя:
    // например, чтобы добавить в вариант «обе ночи» конкретные даты.
    overnightOptions: [
      { value: 'none', label: 'Нет, приедем и уедем', short: 'не ночует' },
      { value: 'fri', label: 'Только с пятницы на субботу', short: 'ночь пт' },
      { value: 'sat', label: 'Только с субботы на воскресенье', short: 'ночь сб' },
      { value: 'both', label: 'Обе ночи', short: 'обе ночи' },
    ],
  },

  // Площадка. Она одна — и на главной, и в приглашении выводится один раз.
  venue: {
    kicker: 'Место торжества',
    name: 'Название площадки',
    address: 'Адрес площадки',
    description: 'Пара слов о месте: как добраться, что рядом, чем оно вам дорого.',
    // Ссылка на карту (Яндекс/Google). Оставьте пустой, чтобы скрыть кнопку.
    mapUrl: '',
  },

  // Расписание по дням. Каждый день — заголовок и список пунктов;
  // так дата не повторяется в каждой строке. text необязателен.
  schedule: [
    {
      date: '15 июня',
      dayOfWeek: 'суббота',
      items: [
        { time: '15:30', title: 'Сбор гостей', text: 'Приезжайте заранее.' },
        { time: '16:00', title: 'Церемония', text: '' },
        { time: 'вечер', title: 'Банкет', text: '' },
      ],
    },
  ],

  // Контакты организаторов / пары.
  // Телефоны берутся из переменных окружения — они тоже не в git.
  contacts: [
    { name: 'Имя', role: 'по любым вопросам', phone: process.env.CONTACT_PHONE || '' },
    { name: 'Имя', role: 'по любым вопросам', phone: process.env.CONTACT_PHONE2 || '' },
  ],
};
