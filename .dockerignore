node_modules
npm-debug.log
.git
.gitignore
.env
.env.*
data
*.md
.DS_Store

# ВНИМАНИЕ: src/config/content.local.js здесь исключать нельзя. Файла нет в git,
# но он нужен внутри образа — без него приложение не стартует. В образ он
# попадает вместе с `COPY src ./src`, то есть берётся с машины, где идёт сборка.
